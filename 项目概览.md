# uv-k1-k5v3-firmware-custom 项目概览

> 生成日期：2026-08-17
> 内容来源：对仓库完整扫描与 README 阅读整理

## 1. 项目是什么

这是**泉盛（Quansheng）UV-K1 / UV-K5 V3 对讲机的第三方定制固件**项目，基于 **PY32F071 MCU**（原版 UV-K5 V1 用的是 DP32G030）。

- **上游血统**：F4HWN（armel）定制固件 → fork 自 Egzumer 固件 → fork 自 DualTachyon 的开创性开源固件
- **当前版本**：Fusion 版 v5.9.0（当前 `main` 分支）
- **远程仓库**：`https://github.com/armel/uv-k1-k5v3-firmware-custom.git`
- **许可证**：Apache 2.0（继承 DualTachyon 原始版权）
- **贡献者**：@muzkr 与 @armel 联合移植，源头致敬 DualTachyon 首次开源的 UV-K5 V1 固件

## 2. Fusion 版主要特性

### 收音与信号处理

- 7 档可调发射功率：Low 1（~20mW）至 High（~5W）+ User 自定义档
- 按 IARU Region 1 建议校准的 S 表（S0 ~ S9+），支持 Classic / Tiny 两种样式
- 可配置 12.5 kHz / 6.25 kHz 窄带 FM
- 每信道 TX 锁定、可调 RX 音量、FM/AM 高级 RX 音频配置文件
- 地区频率锁定配置（业余频段、PMR446、FRS、GMRS、MURS）
- 1600 / 2200 / 3500 mAh 电池配置文件

### 频谱分析仪

- 信道名显示、持久化设置、更快的渲染
- 冻结频谱 / USB 断开行为改进，可开机自动恢复状态

### 扫描

- 最多 24 个命名扫描列表 + `ALL` / `OFF` 分配
- 扫描中直接切换列表、长按 MENU 排除当前信道
- 最多 64 个频率排除项，快速扫描约 150 频率/秒
- 扫描进度、RSSI、CTCSS/DCS 信息显示

### 用户界面

- 改进的 VFO 界面（Classic/Tiny 布局、MAIN ONLY/DUAL/CROSS 显示模式）
- 改进的状态栏、菜单、背光平滑渐变
- 系统信息页：固件版本、电池信息、Flash/SRAM 占用、二维码

### 音频与发射控制

- Classic / OnePush PTT 模式
- 可配置超时计时器提醒（TOT）与发射结束提醒（EOT）
- 收发 Audio Scope、自动深度休眠超时

### Fox Hunt 与信标

- 专用狐狸猎手接收机：校准 S 表、滚动信号历史图、峰值/最低/趋势指示、可选 RF 衰减、静音/盖革/音频模式
- 集成摩斯信标发射机：MOE/MOI/MOS/MOH/MO5/MO 标识、呼号识别、可配置收发周期

### RF 日志

- 自动记录收发活动至外部 Flash：方向、频率、信道名、时长、S 表电平、电池电压
- 机内历史查看 + UV Studio 实时仪表盘 / CSV 导出

### 连接与数据

- UV Studio 集成：屏幕实时串流（K5Viewer）、截图下载、远程键盘控制、自动重连
- 固件刷写、校准数据备份/恢复、boot logo 管理
- BEAM 无线信道整体传输、改进的 AirCopy

### 其他

- 大量菜单增强（SetPwr/SetPTT/SetTOT/SetEOT/SetMet/SetGUI/SetRxA 等）
- 丰富的 F+ 组合键快捷键与可配置按键动作
- 方块游戏 Breakout、VOX、手电筒功能
- 大量可靠性与内存优化（DTMF 呼叫与扰频器在 Fusion 中保持禁用）

## 3. 目录结构

```
uv-k1-k5v3-firmware-custom/
├── App/                  # ★ 应用层（固件核心逻辑）
│   ├── app/              # 应用模块：menu(菜单)、scanner(扫描)、spectrum(频谱仪)、
│   │                     #   fm(收音机)、aircopy、beam、foxhunt(狐狸猎手)、breakout(游戏)、
│   │                     #   rxtx_log(RF日志)、action(按键动作)、dtmf、flashlight 等
│   ├── driver/           # 芯片驱动：bk4819(射频)、bk1080(FM芯片)、st7565(LCD)、
│   │                     #   py25q16(外部Flash)、eeprom、spi/i2c/adc、vcp、keyboard 等
│   ├── helper/           # battery(电池)、boot(启动)
│   ├── ui/               # 界面层：status(状态栏)、menu、inputbox、welcome、lock 等
│   ├── usb/              # USB CDC 虚拟串口 (usbd_cdc_if.c)
│   └── external/         # 第三方库：CMSIS_5(约3234文件)、printf(嵌入式版)
├── Core/                 # MCU 启动/中断/时钟：main.c、py32f071_it.c、system_py32f071.c
├── Drivers/              # PY32F071 HAL 驱动 + CMSIS(含 DSP_Lib、RTOS)
├── Middlewares/CherryUSB # USB 协议栈
├── cmake/                # gcc-arm-none-eabi 交叉编译工具链配置
├── CMakeLists.txt        # 构建系统主配置（C11、arm-none-eabi-gcc、Fusion preset）
├── CMakePresets.json     # CMake 预设（Fusion 等）
├── Dockerfile / Dockerfile-ms / Dockerfile-slim  # Docker 构建环境
├── compile-with-docker.sh # 一键 Docker 编译脚本
├── archive/              # 预编译固件 .bin（约 38 个文件）
│                         #   - f4hwn.fusion.v4.3 ~ v5.9.0 各历史版本
│                         #   - .sa818 版本（SA818 模块支持版）
│                         #   - K1 / K5V3 专属版本
│                         #   - 官方原厂固件备份（stock firmware）
│                         #   - boot logo 图片、rolling development 版
├── tools/
│   ├── serialtool/       # Python 串口烧录/转储/恢复工具
│   ├── unbrick_k5_v1/    # K5 V1 救砖工具（bootloader.bin + 脚本）
│   └── misc/             # 杂项工具（Windows 批处理/编译产物）
├── .github/workflows/    # CI (main.yml)
├── images/               # README 截图素材（界面预览）
├── .gitignore / LICENSE / NOTICE / Doxyfile
└── 项目概览.md            # 本文档
```

## 4. 构建方式

- **编译**：`./compile-with-docker.sh Fusion`（需 Docker + Bash 环境）
  - 首次运行会自动构建 Docker 镜像（`uvk1-uvk5v3`），之后在容器内执行 CMake 编译
  - 产物：`.elf` / `.bin` / `.hex`，输出在 `build/Fusion/`
  - 可附加 CMake 选项，如 `-DENABLE_SPECTRUM=ON`、`-DDEV=ON`（生成滚动开发版）
- **工具链**：`arm-none-eabi-gcc`（C11），预设定义于 `CMakePresets.json`
- **CI**：`.github/workflows/main.yml` 自动构建

## 5. 刷机与使用

- **推荐方式**：浏览器 Web Serial 工具 [UV Studio](https://armel.github.io/uvstudio/)（Flash、校准备份/恢复、K5Viewer、RF Log 一站式）
- **CHIRP**：需使用随 release 附带的专用驱动
- **⚠️ 注意事项**：
  - 刷机前强烈建议立即备份校准数据（`calibration.dat`）
  - 固件无任何保证，可能变砖（brick），风险自负
  - 该对讲机 RX 前端无跟踪调谐带通滤波，强 RF 环境下 AM 接收会明显劣化（硬件局限）

## 6. 当前仓库状态（扫描时快照）

- 分支：`main`，最近提交：`435192e3`（Merge PR #511, v5.9.0 准备）
- ⚠️ 工作区有未提交修改：
  - `archive/*.bin` 大量固件文件为已修改（M）状态
  - `compile-with-docker.sh`、`tools/unbrick_k5_v1/unbrick_k5_v1.sh` 也有改动

## 7. 相关链接

| 项目 | 链接 |
|---|---|
| 本仓库 | https://github.com/armel/uv-k1-k5v3-firmware-custom |
| 上游 F4HWN（UV-K5 V1） | https://github.com/armel/uv-k5-firmware-custom |
| Egzumer 固件 | https://github.com/egzumer/uv-k5-firmware-custom |
| DualTachyon 原始开源固件 | https://github.com/DualTachyon/uv-k5-firmware |
| UV Studio（Web 刷机工具） | https://armel.github.io/uvstudio/ |
| K1 拆机资料 | https://github.com/armel/k1-teardown |
| 使用手册（Wiki） | https://github.com/armel/uv-k1-k5v3-firmware-custom/wiki |
