# 自动多普勒（卫星跟踪）功能使用说明

> 适用固件：本仓库 F4HWN Fusion + 多普勒补丁（`-DENABLE_FEAT_F4HWN_DOPPLER=ON`）
> 适用机型：UV-K1 / UV-K5 V3 / UV-K6 V3（PY32F071 平台）

## 1. 功能原理

与 Losehu 固件（UV-K5 V1）相同的"**外部预生成星历表 + 机内查表**"架构：

1. 网页/工具用 TLE 星历计算出某次卫星过境期间**每秒的上下行频率**（已含多普勒补偿）
2. 通过 USB 串口把数据写入对讲机外部 SPI Flash（`0x1D0000` 区域，16KB）
3. 对讲机上**长按数字 0** 进入多普勒模式 → 输入当前**北京时间（UTC+8）** → 固件按 RTC 时间自动查表：
   - **接收**：自动使用表中下行频率（每秒插值，100 ms 刷新）
   - **发射**：PTT 自动使用表中上行频率 + 预置亚音

## 2. 编译开关

```bash
# 启用多普勒（新增代码约 2.4KB ROM）
cmake --preset Fusion -DENABLE_FEAT_F4HWN_DOPPLER=ON
cmake --build build/Fusion
```

默认关闭，不影响原有固件任何行为。ROM 占用：基线 111.6KB → 114.0KB（可用 118KB，余 6.7KB）。

## 3. 操作步骤

### 3.1 写入星历数据：网页工具 `tools/k5web/index.html`（仿 K5Web）

**使用方法**（Chrome / Edge 浏览器，需支持 Web Serial）：
1. 打开 `tools/k5web/index.html`（可直接双击，或 `python -m http.server` 后访问）
2. **卫星与频率**：点 **⬇️ 获取 TLE** 自动拉取 Celestrak 最新业余卫星星历（通过 GitHub API 镜像 `satvisorcom/satvisor-data`，国内可达，97+ 颗卫星）→ 从下拉选择卫星（如 ISS）→ TLE 与常用上下行频率自动填入；也可手动粘贴 TLE
3. **观测位置**：三种方式——①点 **📍 在地图上选点**（高德地图，点击自动填经纬度 + 自动查询海拔，GCJ-02 已自动转 WGS-84）；②点 **🎯 用我的位置**（浏览器 GPS）；③手动填经纬度/海拔/最低仰角
4. 点 **计算最近过境**：自动用 SGP4 解算未来 24h 内第一次可见过境，生成每秒一条的补偿频率表
5. 用 USB 线连接对讲机 → 点 **连接串口** → 点 **写入星历**（自动执行：擦除 → 写卫星块 → 写频率表，约 10–60 秒，取决于过境时长）

> TLE 数据源：`api.github.com`（CORS 开放）→ 兜底 `cdn.jsdelivr.net`；均不可用时按钮会提示，此时手动粘贴 TLE 即可（示例按钮可先跑通流程）。

写入通过 USB CDC 串口协议（`App/app/uart.c`）：
- `0x05E0` 擦除多普勒数据区（4 个扇区）
- `0x05E1` 写卫星信息块（32 字节，见下方格式）
- `0x05E2` 写频率表条目（索引 + 8 字节，每秒一条）
- 数据布局：
  ```
  0x1D0000  卫星信息块 DOPPLER_Satellite_t（32B）：
    name[10]      卫星名（≤9 字符，name[9]=0）
    start_time[6] 过境开始（年2000起/月/日/时/分/秒）
    end_time[6]   过境结束
    sum_time  u16 总秒数
    send_ctcss u16 发射亚音（Hz/10，0=无）
    start_unix u32 开始时刻（2000-01-01 起秒）
    crc8 + reserved
  0x1D0040  频率表（最多 1920 条 = 32 分钟）：
    uplink u32   上行频率（Hz/10）
    downlink u32 下行频率（Hz/10）
  ```
- 多普勒计算（`tools/k5web/calc.js`）：ECI 惯性系相对速度法，下行 `f×(1-vr/c)`、上行 `f/(1+vr/c)`，已含地球自转
- 单元测试：`tools/k5web/test_protocol.mjs`（协议）、`test_calc.mjs`（计算）、`tools/doppler_test.py`（固件算法），运行 `node test_*.mjs` 即可

### 3.2 对讲机操作

1. **长按数字 0** 进入多普勒模式
   - 注意（v5.9.2 起）：**F+0 短按**仍是原 FM 收音机功能，两个热键并存
2. 屏幕提示 `DATE YYMMDD`：输入 6 位日期（如 `260817` = 2026-08-17）
3. 屏幕提示 `TIME HHMMSS`：输入 6 位时间（如 `100500` = 10:05:00，**输入北京时间 UTC+8**）
   - 非法日期/时间（如 13 月、25 时）会被拒绝：双蜂鸣提示并回到日期输入，重新输入即可
4. 自动进入跟踪界面（与 Losehu 固件卫星屏同款布局，输入时间后直接进入）：
   - 状态行：反白卫星名 + 调制/带宽（FM 25k/12k），右侧保留电量等状态图标
   - 大字：当前接收（下行）频率，过境期间每秒插值、100 ms 刷新；等待时显示当前 VFO 频率
   - S 表行：实时场强 `-91dBm S1` + 右侧刻度信号条（竖线标记为静噪开启门限）
   - 增益行：BK4819 AGC 实时值 LNAs / LNA / PGA / IF（与 Losehu 同源，只读寄存器）
   - 上行行：`UPLink:145.99` + 右侧倒计时（`Long` 还早 / `-320` 距 AOS 秒数 / `+45` 剩余秒数）；
     发射时整行反白显示 `TX:145.99`，右侧显示发射亚音 `T:67.0`；过境结束显示反白 `PASSED`
   - 底行：当前北京时间（每秒刷新）+ 过境进度条（AOS 前 1000 秒逐渐清空，过境中逐渐填满）
5. **PTT** 发射：自动用上行频率 + 预置亚音
6. **EXIT** 退出，恢复原 VFO 频率

### 3.3 注意事项

- 无星历数据时进入模式显示 `NO DATA`，需先写入数据
- 过境结束后显示 `PASSED`，按 EXIT 退出
- 时间断电丢失，但每次开机只需输入一次：退出多普勒模式后 RTC 继续走时，
  再次长按 0 进入会直接开始跟踪，不再要求重输；如需重新对时，在跟踪界面按 **1** 进入微调时间页
- 在微调时间（按 1 进入）、卫星信息（按 2）、槽位总览（按 3）等任意分页按 **M** 立即返回主跟踪页
- 微调时间页：**0** 切换字段（年/月/日/时/分/秒），**1** 加、**2** 减，**M** 或 **EXIT** 保存并返回主跟踪页
- 发射频率以表中数据为准，请确保工具计算的上行频率在合法发射范围内

## 4. 真机测试清单

| # | 测试项 | 预期 |
|---|---|---|
| 1 | 未写数据时长按 0 | 显示 NO DATA |
| 2 | 写入测试数据（正弦波频偏模拟）后长按 0 | 输入日期/时间后可跟踪 |
| 3 | 输入 12 位时间 | 秒内完成，进入跟踪 |
| 4 | 跟踪界面频率显示 | 与写入表一致，100 ms 刷新 |
| 5 | 用频谱/接收验证 RX 频率 | 芯片频率随表变化 |
| 6 | PTT 发射 | 用上行频率，亚音正确 |
| 7 | 发射结束 | 频率恢复下行 |
| 8 | EXIT 退出 | 恢复原 VFO 频率 |
| 9 | 断电重启 | 时间归零，RTC 重新初始化正常 |
| 10 | 板载晶振确认 | 拆机或实测：有无 32.768kHz 晶振（无则走 LSI，误差每分钟约 ±1-3 秒） |
| 11 | 连续过境 30 分钟 | 无崩溃、Flash 读写正常 |

## 5. 相关文件

| 文件 | 说明 |
|---|---|
| `App/app/doppler.h/.c` | 数据层：结构、Flash 读写、时间换算、查表 |
| `App/app/doppler_mode.h/.c` | 模式状态机、UI、按键、发射 |
| `App/driver/rtc.h/.c` | PY32F071 RTC 驱动（LSE/LSI + 1 秒中断） |
| `Core/Src/py32f071_it.c` | RTC 中断处理 |
| `App/app/uart.c` | 串口写星历命令（0x05E0-0x05E2） |
| `App/app/main.c` | 长按 0 入口（processFKeyFunction） |
| `App/ui/ui.h`、`App/ui/ui.c`、`App/app/app.c` | 显示/按键/主循环挂接 |
| `CMakePresets.json`、`App/CMakeLists.txt` | 编译开关 |
| `tools/k5web/index.html` | 网页写星历工具（仿 K5Web，Web Serial） |
| `tools/k5web/app.js` | 浏览器逻辑（计算+写入流程） |
| `tools/k5web/calc.js` | 多普勒计算模块（UMD，含单测 test_calc.mjs） |
| `tools/k5web/protocol.js` | 串口协议模块（UMD，含单测 test_protocol.mjs） |
| `tools/k5web/vendor/satellite.min.js` | SGP4/TLE 库（satellite.js，本地化） |
| `tools/doppler_test.py` | 固件算法 PC 端逻辑单元测试 |
