# 自动多普勒（卫星跟踪）功能移植方案

> 版本：v1.0 ｜ 日期：2026-08-17 ｜ 目标固件：F4HWN Fusion v5.9.0（本仓库）
> 目标机型：泉盛 UV-K1 / UV-K5 V3 / **UV-K6 V3**（同平台：PY32F071xB + BK4829 + PY25Q16 2MB SPI Flash）

## 1. 目标

为 UV-K6 V3（PY32F071 平台）固件增加**自动卫星多普勒跟踪**功能，参考 Losehu 固件（UV-K5 V1 / DP32G030）的成熟架构，但完全按本项目的硬件与代码框架实现：

- **外部工具预生成星历表**（用户已确认选此方案）：网页工具用 TLE 星历预计算卫星过境期间每秒的上下行频率（已含多普勒补偿），写入对讲机外部 SPI Flash
- **固件按 RTC 时间查表自动跟踪**：进入多普勒模式 → 手动输入当前时间 → 固件按时间查表，每秒插值、100 ms 刷新 RX 频率；PTT 发射自动使用表内上行频率（含 CTCSS）
- **时间来源**：开机/进入模式时手动输入（写入 PY32F071 内部 RTC，断电丢失可接受）

## 2. 硬件事实与 ROM 预算（调研确认）

| 项目 | 数值 | 说明 |
|---|---|---|
| MCU | PY32F071xB（Cortex-M0+，72MHz） | 链接脚本 `Core/py32f071xb.ld` |
| 内部 Flash | 128KB，**可用 118KB**（0x08002800 起，前 10KB bootloader） | 程序代码唯一存放处 |
| 内部 RAM | 16KB | 数据 |
| 外部 SPI Flash | PY25Q16HB，**2MB** | 数据（设置/日志/未来星历表） |
| 当前固件占用 | v5.9.0 release = 111,584B ≈ **109.0KB** | ROM 余量约 **9KB**（dev 版约 6.4KB） |
| 射频芯片 | BK4829（寄存器兼容 BK4819） | 驱动已在仓库 |
| RTC | PY32F071 自带 RTC 外设，本固件**无任何 RTC 代码** | 需从零实现；板载 32.768kHz 晶振有无**待真机确认** |

**结论**：ROM 余量 ~9KB 是唯一硬约束。多普勒功能预算 ~6-8KB，需要 `-Os` + 必要时裁剪可选功能（GAME/FOXHUNT/RXTX_LOG/QRCODE 等 CMake 开关可关）。**ROM 溢出只会导致链接失败，不会产出坏固件，无变砖风险**；变砖风险仅存在于刷错硬件版本固件（V1=DP32G030 / V2=PY32F030 / V3=PY32F071），本方案只针对 V3 平台。

## 3. 外部 Flash 存储布局（新增数据区）

现状布局（`App/driver/eeprom_compat.c` 映射表 + `rxtx_log.c`）：

```
0x000000 ~ 0x00A170   设置/信道/VFO/版本（已占用）
0x010000 ~ 0x010200   校准数据（已占用）
0x011000 ~ 0x012000   Boot Logo 扇区（已占用）
0x1E0000 ~ 0x1E8000   RF 日志 32KB（已占用）
```

**新增多普勒数据区：`0x1D0000 ~ 0x1D4000`（16KB，4 个 4KB 扇区，与 RF Log 相隔 64KB 缓冲区）**

```
0x1D0000            卫星信息块（32B）：
  name[10]          卫星名（≤9 字符 + '\0'）
  start_time[6]     过境开始（年/月/日/时/分/秒，BCD 十位个位）
  end_time[6]       过境结束
  sum_time   u16    总过境秒数
  send_ctcss u16    发射亚音（0=无）
  start_unix u32    过境开始 UNIX 秒（基准 2000-01-01 00:00）
0x1D0040            频率表（每秒一条，8B/条，最多 1920 条 ≈ 32 分钟）：
  uplink    u32     上行频率（Hz/10）——PTT 发射用
  downlink  u32     下行频率（Hz/10）——接收用
```

- 数据区首字节放 `0xAA` 魔数 + CRC8 校验，无效/未写入时固件提示"无星历数据"
- 写入流程：擦除 4 个扇区 → 写卫星信息块 → 写频率表（按 256B 页写）

## 4. 模块设计（全部走 CMake 开关 `ENABLE_FEAT_F4HWN_DOPPLER`，默认 OFF）

### 4.1 数据层 `App/app/doppler.c` / `doppler.h`（核心逻辑，仿 Losehu 但独立实现）

```c
typedef struct {
    char     name[10];
    uint8_t  start_time[6];
    uint8_t  end_time[6];
    uint16_t sum_time;
    uint16_t send_ctcss;
    uint32_t start_unix;
} DOPPLER_Satellite_t;          // 32B

typedef struct {
    uint32_t uplink;            // Hz/10
    uint32_t downlink;          // Hz/10
} DOPPLER_Entry_t;              // 8B，每秒一条

void DOPPLER_Init(void);                        // 读卫星信息块，校验魔数
bool DOPPLER_HasData(void);                     // 是否有有效星历
bool DOPPLER_GetEntry(int32_t unixNow, DOPPLER_Entry_t *e);  // 查表（返回是否有当前条目）
uint32_t DOPPLER_UnixTime(const uint8_t t[6]);  // 年月日时分秒 → 2000 基准秒
bool DOPPLER_WriteSatellite(const DOPPLER_Satellite_t *s);  // 写信息块（含擦除+魔数）
bool DOPPLER_WriteEntry(uint16_t idx, const DOPPLER_Entry_t *e); // 写单条（串口工具用，页缓冲）
```

- 时间换算：2000-01-01 起算的累计秒（闰年/每月天数），无浮点
- 查表：`idx = unixNow - start_unix`（每秒一条），超出 `sum_time` 范围返回 false；另有插值接口 `DOPPLER_GetEntryInterpolated(unixNow, ms)` 供 100 ms 刷新

### 4.2 RTC 驱动 `App/driver/rtc.c` / `rtc.h`（从零写，寄存器级或 LL）

- PY32F071 RTC：时钟源优先 LSE（32.768kHz 晶振，**真机确认有无**），否则 LSI（内部 RC，需校准说明）
- 提供 `RTC_Init()`、`RTC_SetDateTime(const uint8_t t[6])`、`RTC_GetDateTime(uint8_t t[6])`
- 1 秒中断：RTC 闹钟/周期中断 → 置标志 `gDopplerTick = true`（主循环消费，避免中断里干重活）
- 寄存器级实现，避免拉入整个 HAL（省 ROM）；若 HAL RTC 代码更省则用 HAL（编译后看 .map 决定）

### 4.3 多普勒模式 `App/app/doppler_mode.c`（状态机 + 界面，省 ROM 优先）

```
状态：DOPPLER_STATE_IDLE → ENTER_TIME（输入当前时间，复用 gInputBox 8 位）→ TRACKING
```

- 进入方式：快捷键（F+ 组合键，实施时选未被占用的组合，如 **F+Down** 或 **F+0**，与现有功能冲突则换）
- TRACKING 显示（简化版，复用现有字体/绘制函数）：
  - 第 1 行：卫星名 + 过境倒计时（`-N s` / `+N s` / `Passed`）
  - 第 2 行：当前 RX 频率（下行，每秒插值、100 ms 刷新）
  - 第 3 行：当前 TX 频率（上行）+ CTCSS 亚音号
  - 第 4 行：进度条（复用 UI 条状绘制）
- 无有效数据时：显示"NO DATA"提示
- 发射链路：TRACKING 状态按 PTT 时，用表内 `uplink` 频率发射（走 `RADIO_PrepareTX` 前临时替换 `pTX->Frequency`，发完恢复），CTCSS 按 `send_ctcss` 设置（`BK4819_SetCTCSSFrequency`）
- 退出：EXIT 键返回主界面，恢复原 VFO 频率
- 每 100 ms：多普勒直接用绝对频率覆盖 `gTxVfo->freq_config_RX.Frequency` 并 `BK4819_SetFrequency`（基于 1 s 表线性插值），退出时恢复

### 4.4 串口写入协议（网页工具 → 对讲机）

- 复用现有 USB CDC（`App/usb/usbd_cdc_if.c` + `App/app/uart.c` 协议框架），新增私有命令（如以 `0x7E` 帧或现有协议扩展）：
  - `CMD_DOPPLER_WRITE_SAT`（写卫星信息块 + 自动擦扇区）
  - `CMD_DOPPLER_WRITE_ENTRY`（写频率表条目，带内部页缓冲）
  - `CMD_DOPPLER_READ_BACK`（回读校验，供工具确认）
- 不启用 USB 时（ENABLE_USB=OFF 构建）此功能自动禁用

### 4.5 网页工具（仿 K5Web，独立阶段）

- 静态单页 HTML+JS：TLE 数据源（Celestrak 拉取/粘贴）→ SGP4 解算（内嵌 sgp4.js 或简化解算）→ 计算过境窗口与每秒多普勒频率 → Web Serial 通过 4.4 协议写入
- 工作量最大，独立于固件开发，可后置

## 5. 代码量估算与 ROM 预算

| 模块 | 预估 ROM | 说明 |
|---|---|---|
| 数据层 doppler.c | ~1.5KB | 纯查表逻辑 |
| RTC 驱动 | ~1.5KB | 寄存器级 |
| 多普勒模式 + UI | ~3KB | 简化界面 |
| 串口协议扩展 | ~1KB | 3 条命令 |
| 合计 | **~7KB** | 卡在 ~9KB 预算内 |

若链接超限：裁剪优先级 GAME(breakout) > QRCODE > RXTX_LOG > FOXHUNT（各可省 1-3KB），最终以 .map 实测为准。

## 6. 实施阶段（Todo 列表）

1. **基线编译**：Docker 编译当前 main，取 .map 精确 ROM/RAM 占用（后台进行中）
2. **存储布局**：确认 0x1D0000 区域空闲（对照 eeprom_compat.c 全表）
3. **数据层**：doppler.h/doppler.c（结构/读写/时间换算/查表）
4. **RTC 驱动**：rtc.c/h + 1 秒中断挂接
5. **多普勒模式**：状态机 + UI + 时间输入（复用 inputbox）
6. **快捷键入口**：F+ 组合键接入（检查占用）
7. **发射链路**：PTT 上行频率 + CTCSS
8. **串口协议**：写星历 3 条命令
9. **构建验证**：ENABLE_DOPPLER=ON 编译，比对 .map，超限则裁剪；逻辑单元测试（时间换算/查表边界，PC 端跑）
10. **文档**：使用说明 + 真机测试清单（含"板载晶振确认"项）
11. **网页工具**（独立阶段）：仿 K5Web 写星历

## 7. 风险清单

| 风险 | 等级 | 应对 |
|---|---|---|
| ROM 溢出（~9KB 预算） | 中 | -Os + 裁剪开关；精确数据以 .map 为准 |
| 板载无 32.768kHz 晶振 | 中 | LSI 内部 RC 兜底（误差累计，需对时容忍 ±30s），真机确认 |
| 频率表 32 分钟上限 | 低 | 与 Losehu 一致；超长过境工具侧截断 |
| 写 Flash 磨损 | 低 | 每次写入前整体擦 4 扇区，写入频率低（每次过境前 1 次） |
| 与现有 F+ 组合键冲突 | 低 | 实施时查 action.c 全表选空闲组合 |
| BK4829 与 BK4819 亚音 API 差异 | 低 | 用本项目现有 `BK4819_SetCTCSSFrequency`（兼容） |

## 8. 验证方式（无模拟器，如实说明）

- 本生态**不存在** PC 固件模拟器（仅 sq5bpf/k5emulator 协议模拟器，不执行固件代码）
- **逻辑层**：时间换算/查表边界逻辑提取到 PC 用 Python 复算对照 + C 单元测试
- **真机**：UV Studio/K5Viewer 屏幕镜像 + 远程按键验证 UI；构造测试星历数据（脚本生成正弦波频偏模拟数据）验证查表与发射频率切换
