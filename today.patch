diff --git a/App/app/menu.c b/App/app/menu.c
index 2e59fe7..d2d410f 100644
--- a/App/app/menu.c
+++ b/App/app/menu.c
@@ -469,6 +469,11 @@ void MENU_AcceptSetting(void)
 
     switch (UI_MENU_GetCurrentMenuId())
     {
+        
+        case MENU_SELCAL_CODE: //Les digits sont déjà dans gEeprom.selective_tx_seq[], 
+        
+            break;
+
         default:
             return;
 
@@ -1706,9 +1711,18 @@ static void MENU_Key_MENU(const bool bKeyPressed, const bool bKeyHeld)
             edit_index          = -1;
         }
 
+        if (UI_MENU_GetCurrentMenuId() == MENU_SELCAL_CODE)
+        {
+            // Quand on entre dans SelSeq, on commence au le premier digit/
+            gSelcalCursor = 0;
+        }
+
         return;
     }
 
+    
+
+
     if (UI_MENU_GetCurrentMenuId() == MENU_MEM_NAME)
     {
         if (edit_index < 0)
@@ -1833,6 +1847,19 @@ static void MENU_Key_STAR(const bool bKeyPressed, const bool bKeyHeld)
         return;
     }
 
+    if (UI_MENU_GetCurrentMenuId() == MENU_SELCAL_CODE && gIsInSubMenu)
+    {
+        // Avance le curseur sur le digit suivant (0..4 -> boucle)
+        if (gSelcalCursor < 4)
+            gSelcalCursor++;
+        else
+            gSelcalCursor = 0;
+
+        gRequestDisplayScreen = DISPLAY_MENU;
+        return;
+    }
+
+
     RADIO_SelectVfos();
 
     #ifdef ENABLE_NOAA
@@ -1884,6 +1911,29 @@ static void MENU_Key_UP_DOWN(bool bKeyPressed, bool bKeyHeld, int8_t Direction)
         return;
     }
 
+    if (UI_MENU_GetCurrentMenuId() == MENU_SELCAL_CODE && gIsInSubMenu)
+    {
+        // UP/DOWN changent la valeur du digit courant (0..9)
+        if (bKeyPressed && Direction != 0)
+        {
+            uint8_t v = gEeprom.selective_tx_seq[gSelcalCursor];
+
+            if (Direction < 0) {
+                // UP -> +1 modulo 10
+                v = (uint8_t)((v + 1) % 10);
+            } else { // Direction > 0
+                // DOWN -> -1 modulo 10
+                v = (v == 0) ? 9 : (uint8_t)(v - 1);
+            }
+
+            gEeprom.selective_tx_seq[gSelcalCursor] = v;
+            gRequestDisplayScreen = DISPLAY_MENU;
+            gBeepToPlay = BEEP_1KHZ_60MS_OPTIONAL;
+        }
+        return;
+    }
+
+
     if (!bKeyHeld)
     {
         if (!bKeyPressed)
diff --git a/App/app/selective_call.c b/App/app/selective_call.c
index 0ab0cc1..f528366 100644
--- a/App/app/selective_call.c
+++ b/App/app/selective_call.c
@@ -7,6 +7,7 @@
 #include "driver/system.h"
 #include "ui/ui.h"
 #include "audio.h"
+#include "settings.h"
 
 // ------------------------------------------------------
 // Tables des fréquences ZVEI-1, ZVEI-2, CCIR
@@ -153,7 +154,7 @@ void SelectiveCall_ProcessTone(uint8_t tone)
 }
 
 // ------------------------------------------------------
-// Vérifie correspondance RX avec séquence attendue
+// Vérifie la  correspondance RX avec séquence attendue
 // ------------------------------------------------------
 bool SelectiveCall_Match(void)
 {
@@ -167,3 +168,25 @@ bool SelectiveCall_Match(void)
 
     return true;
 }
+// ------------------------------------------------------
+// Helper : envoi SelCall basé sur la config globale EEPROM
+// ------------------------------------------------------
+void SelectiveCall_SendFromEeprom(void)
+{
+    // Si le SelCall est désactivé, on ne fait rien
+    if (gEeprom.selective_mode == 0)   // SELECTIVE_OFF
+        return;
+
+    selective_conf_t conf;
+    memset(&conf, 0, sizeof(conf));
+
+    conf.mode = (selective_mode_t)gEeprom.selective_mode;
+
+    // Pour l’instant : on utilise la même séquence en TX et RX
+    for (uint8_t i = 0; i < SELECTIVE_MAX_TONES; i++) {
+        conf.tx_seq[i] = gEeprom.selective_tx_seq[i];
+        conf.rx_seq[i] = gEeprom.selective_tx_seq[i];
+    }
+
+    SelectiveCall_Send(&conf);
+}
diff --git a/App/app/selective_call.h b/App/app/selective_call.h
index a2c8c0a..4c0fe66 100644
--- a/App/app/selective_call.h
+++ b/App/app/selective_call.h
@@ -36,5 +36,7 @@ void SelectiveCall_Send(const selective_conf_t *conf);
 void SelectiveCall_ResetRx(void);
 void SelectiveCall_ProcessTone(uint8_t tone);
 bool SelectiveCall_Match(void);
+void SelectiveCall_SendFromEeprom(void);
+
 
 #endif
diff --git a/App/radio.c b/App/radio.c
index 055fb26..0868c39 100644
--- a/App/radio.c
+++ b/App/radio.c
@@ -1185,6 +1185,19 @@ void RADIO_PrepareTX(void)
 
     // TX is allowed
 
+    // ------------------------------------------
+    // Selective Call (ZVEI / CCIR / REGA)
+    // ------------------------------------------
+    if (gEeprom.selective_mode > 0)
+    {
+        selective_conf_t cfg = {0};
+        cfg.mode = gEeprom.selective_mode;
+        SelectiveCall_Send(&cfg);
+
+        // petit délai pour laisser terminer le dernier ton
+        SYSTEM_DelayMs(50);
+    }
+
 #ifdef ENABLE_DTMF_CALLING
     if (gDTMF_ReplyState == DTMF_REPLY_ANI)
     {
diff --git a/App/settings.c b/App/settings.c
index 6291da7..7cca02e 100644
--- a/App/settings.c
+++ b/App/settings.c
@@ -394,6 +394,12 @@ void SETTINGS_InitEEPROM(void)
         gSetting_set_ptt_session = gSetting_set_ptt;
         gEeprom.KEY_LOCK_PTT = gSetting_set_lck;
     #endif
+
+        gEeprom.selective_mode = 0;   // SELECTIVE_OFF
+
+        for (int i = 0; i < SELECTIVE_MAX_TONES; i++) {
+        gEeprom.selective_tx_seq[i] = 0;   // "00000" par défaut
+    }
 }
 
 void SETTINGS_LoadCalibration(void)
diff --git a/App/settings.h b/App/settings.h
index 595d31a..ad84987 100644
--- a/App/settings.h
+++ b/App/settings.h
@@ -24,6 +24,8 @@
 #include <helper/battery.h>
 #include "radio.h"
 #include <driver/backlight.h>
+#define SELECTIVE_MAX_TONES 5
+
 
 enum POWER_OnDisplayMode_t {
 #ifdef ENABLE_FEAT_F4HWN
@@ -303,6 +305,7 @@ typedef struct {
     uint8_t               S9_LEVEL;
 #endif
     uint8_t               selective_mode; 
+    uint8_t               selective_tx_seq[SELECTIVE_MAX_TONES]; 
 } EEPROM_Config_t;
 
 extern EEPROM_Config_t gEeprom;
diff --git a/App/ui/menu.c b/App/ui/menu.c
index 9d40038..c94a4ce 100644
--- a/App/ui/menu.c
+++ b/App/ui/menu.c
@@ -31,6 +31,7 @@
 #include "../helper/battery.h"
 #include "../misc.h"
 #include "../settings.h"
+#include "settings.h"
 
 #ifdef ENABLE_FEAT_F4HWN
     #include "../version.h"
@@ -133,6 +134,7 @@ const t_menu_item MenuList[] =
 #endif
 
     {"SelCal",     MENU_SELECTIVE   },
+    {"SelSeq",     MENU_SELCAL_CODE },
     {"VOX",         MENU_VOX           },
 #ifdef ENABLE_FEAT_F4HWN
     {"SysInf",      MENU_VOL           }, // was "VOL"
@@ -504,7 +506,7 @@ uint8_t UI_MENU_GetMenuIdx(uint8_t id)
 }
 
 int32_t gSubMenuSelection;
-
+uint8_t gSelcalCursor = 0;   // 0..4 : digit position for edition pour le selseq
 // edit box
 char    edit_original[17]; // a copy of the text before editing so that we can easily test for changes/difference
 char    edit[17];
@@ -691,8 +693,33 @@ void UI_DisplayMenu(void)
         case MENU_SELECTIVE:
             strcpy(String, gSubMenu_SELCAL[gSubMenuSelection]);
             break;
+            
+        case MENU_SELCAL_CODE:
+        {
+            // Affichera les 5 digits de la séquence du SelCall
+            sprintf(String, "%u %u %u %u %u",
+                gEeprom.selective_tx_seq[0],
+                gEeprom.selective_tx_seq[1],
+                gEeprom.selective_tx_seq[2],
+                gEeprom.selective_tx_seq[3],
+                gEeprom.selective_tx_seq[4]
+            );
+
+            // Ligne “texte” de la séquence
+            UI_PrintString(String, menu_item_x1, menu_item_x2, 2, 8);
+
+            // Position du curseur ^ sous le digit sélectionné
+            // "0 0 0 0 0" -> chaque digit + espace  = 2 colonnes
+            uint8_t cursor_col = gSelcalCursor * 2;   // 0, 2, 4, 6, 8
+            uint8_t cursor_x   = menu_item_x1 + (cursor_col * 8);
 
+            UI_PrintString("^", cursor_x, menu_item_x2, 4, 8);
+
+            already_printed = true;
+            break;
+        }
 
+        
         case MENU_SFT_D:
             strcpy(String, gSubMenu_SFT_D[gSubMenuSelection]);
             break;
diff --git a/App/ui/menu.h b/App/ui/menu.h
index 5cab098..a0f5b83 100644
--- a/App/ui/menu.h
+++ b/App/ui/menu.h
@@ -99,6 +99,7 @@ enum
 #endif
     MENU_D_LIVE_DEC,
     MENU_SELECTIVE,
+    MENU_SELCAL_CODE,
     MENU_PONMSG,
     MENU_ROGER,
     MENU_VOL,
@@ -187,6 +188,7 @@ extern const char        gSubMenu_D_RSP[4][11];
 #ifdef ENABLE_FEAT_F4HWN
     extern const char    gSubMenu_SET_PWR[7][6];
     extern const char    gSubMenu_SET_PTT[2][8];
+    extern const char * const gSubMenu_SELCAL[6]; 
     extern const char    gSubMenu_SET_TOT[4][7];
     extern const char    gSubMenu_SET_LCK[2][9];
     extern const char    gSubMenu_SET_MET[2][8];
diff --git a/App/ui/ui.h b/App/ui/ui.h
index 0f1fea4..9be127a 100644
--- a/App/ui/ui.h
+++ b/App/ui/ui.h
@@ -50,6 +50,7 @@ extern GUI_DisplayType_t gRequestDisplayScreen;
 extern uint8_t           gAskForConfirmation;
 extern bool              gAskToSave;
 extern bool              gAskToDelete;
+extern uint8_t gSelcalCursor;
 
 void GUI_DisplayScreen(void);
 void GUI_SelectNextDisplay(GUI_DisplayType_t Display);
diff --git a/diff_bk4819_c.diff b/diff_bk4819_c.diff
deleted file mode 100644
index a516a48..0000000
--- a/diff_bk4819_c.diff
+++ /dev/null
@@ -1,51 +0,0 @@
-diff --git a/App/driver/bk4819.c b/App/driver/bk4819.c
-index 89bf3df..8b53a1a 100644
---- a/App/driver/bk4819.c
-+++ b/App/driver/bk4819.c
-@@ -27,6 +27,9 @@
- #include "driver/system.h"
- #include "driver/systick.h"
- 
-+#include "selective_call.h"
-+
-+
- 
- #ifndef ARRAY_SIZE
-     #define ARRAY_SIZE(x) (sizeof(x) / sizeof(x[0]))
-@@ -1853,3 +1856,36 @@ void BK4819_PlayDTMFEx(bool bLocalLoopback, char Code)
- 
-     BK4819_ExitTxMute();
- }
-+void BK4819_SendZVEI2Sequence(const uint16_t *tones, uint8_t len)
-+{
-+    // CONFIGURATION : gains & paramètres Tone1
-+    BK4819_EnterTxMute();
-+    BK4819_SetAF(BK4819_AF_MUTE);
-+
-+    // Gain correct pour les 5 tons (équivalent Motorola / Rega)
-+    BK4819_WriteRegister(BK4819_REG_70,
-+        BK4819_REG_70_ENABLE_TONE1 |
-+        (66u << BK4819_REG_70_SHIFT_TONE1_TUNING_GAIN));
-+
-+    BK4819_EnableTXLink();
-+    SYSTEM_DelayMs(50);
-+
-+    for (uint8_t i = 0; i < len; i++)
-+    {
-+        uint16_t freq = tones[i];
-+
-+        // jouer un ton ZVEI2
-+        BK4819_WriteRegister(BK4819_REG_71, scale_freq(freq));
-+        BK4819_ExitTxMute();
-+        SYSTEM_DelayMs(70);  // durée ton ZVEI standard
-+
-+        // pause entre les tons
-+        BK4819_EnterTxMute();
-+        SYSTEM_DelayMs(10);
-+    }
-+
-+    // Arrêt transmission
-+    BK4819_WriteRegister(BK4819_REG_70, 0);
-+    BK4819_WriteRegister(BK4819_REG_30, 0xC1FE);
-+    BK4819_ExitTxMute();
-+}
diff --git a/diff_eeprom_c.diff b/diff_eeprom_c.diff
deleted file mode 100644
index e69de29..0000000
diff --git a/diff_menu_h.diff b/diff_menu_h.diff
deleted file mode 100644
index 0004565..0000000
--- a/diff_menu_h.diff
+++ /dev/null
@@ -1,12 +0,0 @@
-diff --git a/App/ui/menu.h b/App/ui/menu.h
-index f8bfb1a..5cab098 100644
---- a/App/ui/menu.h
-+++ b/App/ui/menu.h
-@@ -98,6 +98,7 @@ enum
-     MENU_D_LIST,
- #endif
-     MENU_D_LIVE_DEC,
-+    MENU_SELECTIVE,
-     MENU_PONMSG,
-     MENU_ROGER,
-     MENU_VOL,
diff --git a/diff_settings_c.diff b/diff_settings_c.diff
deleted file mode 100644
index e69de29..0000000
diff --git a/diff_settings_h.diff b/diff_settings_h.diff
deleted file mode 100644
index 376bc66..0000000
--- a/diff_settings_h.diff
+++ /dev/null
@@ -1,12 +0,0 @@
-diff --git a/App/settings.h b/App/settings.h
-index 35639b5..595d31a 100644
---- a/App/settings.h
-+++ b/App/settings.h
-@@ -302,6 +302,7 @@ typedef struct {
-     uint8_t               S0_LEVEL;
-     uint8_t               S9_LEVEL;
- #endif
-+    uint8_t               selective_mode; 
- } EEPROM_Config_t;
- 
- extern EEPROM_Config_t gEeprom;
diff --git a/diff_ui_menu_c.diff b/diff_ui_menu_c.diff
deleted file mode 100644
index 8833b55..0000000
--- a/diff_ui_menu_c.diff
+++ /dev/null
@@ -1,43 +0,0 @@
-diff --git a/App/ui/menu.c b/App/ui/menu.c
-index 46e57e3..9d40038 100644
---- a/App/ui/menu.c
-+++ b/App/ui/menu.c
-@@ -131,6 +131,8 @@ const t_menu_item MenuList[] =
-         {"AM Fix",      MENU_AM_FIX        },
-     #endif
- #endif
-+
-+    {"SelCal",     MENU_SELECTIVE   },
-     {"VOX",         MENU_VOX           },
- #ifdef ENABLE_FEAT_F4HWN
-     {"SysInf",      MENU_VOL           }, // was "VOL"
-@@ -202,6 +204,17 @@ const char gSubMenu_TXP[][6] =
-     "HIGH"
- };
- 
-+const char * const gSubMenu_SELCAL[] =
-+{
-+    "OFF",
-+    "ZVEI-1",
-+    "ZVEI-2",
-+    "CCIR",
-+    "REGA TEST",
-+    "REGA ALARM"
-+};
-+
-+
- const char gSubMenu_SFT_D[][4] =
- {
-     "OFF",
-@@ -675,6 +688,11 @@ void UI_DisplayMenu(void)
-             break;
-         }
- 
-+        case MENU_SELECTIVE:
-+            strcpy(String, gSubMenu_SELCAL[gSubMenuSelection]);
-+            break;
-+
-+
-         case MENU_SFT_D:
-             strcpy(String, gSubMenu_SFT_D[gSubMenuSelection]);
-             break;
diff --git a/diff_ui_menu_full.diff b/diff_ui_menu_full.diff
deleted file mode 100644
index 8833b55..0000000
--- a/diff_ui_menu_full.diff
+++ /dev/null
@@ -1,43 +0,0 @@
-diff --git a/App/ui/menu.c b/App/ui/menu.c
-index 46e57e3..9d40038 100644
---- a/App/ui/menu.c
-+++ b/App/ui/menu.c
-@@ -131,6 +131,8 @@ const t_menu_item MenuList[] =
-         {"AM Fix",      MENU_AM_FIX        },
-     #endif
- #endif
-+
-+    {"SelCal",     MENU_SELECTIVE   },
-     {"VOX",         MENU_VOX           },
- #ifdef ENABLE_FEAT_F4HWN
-     {"SysInf",      MENU_VOL           }, // was "VOL"
-@@ -202,6 +204,17 @@ const char gSubMenu_TXP[][6] =
-     "HIGH"
- };
- 
-+const char * const gSubMenu_SELCAL[] =
-+{
-+    "OFF",
-+    "ZVEI-1",
-+    "ZVEI-2",
-+    "CCIR",
-+    "REGA TEST",
-+    "REGA ALARM"
-+};
-+
-+
- const char gSubMenu_SFT_D[][4] =
- {
-     "OFF",
-@@ -675,6 +688,11 @@ void UI_DisplayMenu(void)
-             break;
-         }
- 
-+        case MENU_SELECTIVE:
-+            strcpy(String, gSubMenu_SELCAL[gSubMenuSelection]);
-+            break;
-+
-+
-         case MENU_SFT_D:
-             strcpy(String, gSubMenu_SFT_D[gSubMenuSelection]);
-             break;
